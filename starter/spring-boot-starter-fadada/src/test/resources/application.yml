spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: 20160826
  main:
    banner-mode: "off"
  application:
    name: "spring-boot-starter-fadada"
  cloud:
    nacos:
      discovery:
        event:
          enabled: true
          instance-registered-subscribe: true
        ip: ${NACOS_DISCOVERY_IP:}
        server-addr: ${NACOS_HOST:localhost:8848}
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD:Cxb@20160826}
        group: ${NACOS_DISCOVERY_GROUP:NEIGHBORLY}
        metadata:
          version: ${info.version}
          name: ${info.name}
          artifact-id: ${info.artifact-id}
          group-id: ${info.group-id}
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        username: ${spring.cloud.nacos.discovery.username}
        password: ${spring.cloud.nacos.discovery.password}
        file-extension: yml
        group: ${NACOS_CONFIG_GROUP:NEIGHBORLY}
        extension-configs:
          - data-id: application.${spring.cloud.nacos.config.file-extension}
            refresh: true
            group: ${NACOS_APPLICATION_GROUP:NEIGHBORLY}

dactiv:
  fadada:
    secret:
      secret-id: 80002872
      secret-key: LXFPTXWDJHQF4HUPM0G4DSTPE7LYTE9I
    client-corp-id: 1903980686920245248
    open-corp-id: 6cd4cc94e4a545e5a02c521fb174d15e
    base-url: https://uat-api.fadada.com/api/v5
